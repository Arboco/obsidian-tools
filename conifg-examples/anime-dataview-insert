
```dataview
table without id
	embed(link(meta(cover-img).path, "100")) as "Cover",
	"[[" + file.name + " | " + substring(file.name, 0, 25) + choice(length(file.name) > 25, "...]]", "]]"), 
	"Episode: " + episode, 
	date,
	file.size
Where contains(origin, this.file.link)
Sort episode ASC
```
